name: Update Postman Collection

on:
  release:
    types:
      - created

jobs:
  update_postman:
    if: ${{ github.event.release.prerelease == false }}
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Postman Collections
        run: |
          echo "🆕 Creating Application collection..."
          curl --location --request POST 'https://api.getpostman.com/collections?workspace={{secrets.POSTMAN_WORKSPACE_ID}}' \
            --header 'Content-Type: application/json' \
            --header 'X-Api-Key: {{secrets.POSTMAN_API_KEY}}' \
            --data-binary "@postman/application.json"
          echo "\n✅ Application collection created!"

          echo "🆕 Creating Platform collection..."
          curl --location --request POST 'https://api.getpostman.com/collections?workspace={{secrets.POSTMAN_WORKSPACE_ID}}' \
            --header 'Content-Type: application/json' \
            --header 'X-Api-Key: {{secrets.POSTMAN_API_KEY}}' \
            --data-binary "@postman/platform.json"
          echo "\n✅ Platform collection created!"
