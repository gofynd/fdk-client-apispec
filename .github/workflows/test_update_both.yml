name: Test - Update Both

on:
  workflow_dispatch:

jobs:
  update_postman:
    if: ${{ github.event.release.prerelease == false }}
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Update Postman Collections
        run: |
          echo "🔁 Updating Application collection..."
          curl --location --request PUT 'https://api.getpostman.com/collections/46463610-878f8133-f560-4e06-8494-7fa15a7e2595' \
            --header 'Content-Type: application/json' \
            --header 'X-Api-Key: ${{ secrets.POSTMAN_API_KEY }}' \
            --data-binary "@postman/application.json"
          echo "✅ Application updated!"

          echo "🔁 Updating Platform collection..."
          curl --location --request PUT 'https://api.getpostman.com/collections/46463610-a450109c-3dd9-4738-8382-c869bff9527d' \
            --header 'Content-Type: application/json' \
            --header 'X-Api-Key: ${{ secrets.POSTMAN_API_KEY }}' \
            --data-binary "@postman/platform.json"
          echo "✅ Platform updated!"
