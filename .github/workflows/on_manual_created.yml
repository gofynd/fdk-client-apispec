name: Manual Postman Collection

on:
  workflow_dispatch:

jobs:
  update_postman:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Create Postman Collections
        run: |
          echo "🆕 Creating Application collection..."
          curl --location --request POST "https://api.getpostman.com/collections?workspace=$POSTMAN_WORKSPACE_ID" \
            --header "Content-Type: application/json" \
            --header "X-Api-Key: $POSTMAN_API_KEY" \
            --data-binary "@postman/application.json"
          echo "✅ Application collection created!"

          echo "🆕 Creating Platform collection..."
          curl --location --request POST "https://api.getpostman.com/collections?workspace=$POSTMAN_WORKSPACE_ID" \
            --header "Content-Type: application/json" \
            --header "X-Api-Key: $POSTMAN_API_KEY" \
            --data-binary "@postman/platform.json"
          echo "✅ Platform collection created!"
        env:
          POSTMAN_WORKSPACE_ID: ${{ secrets.POSTMAN_WORKSPACE_ID }}
          POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
